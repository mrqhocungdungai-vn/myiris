## 1. Preconditions

- [ ] 1.1 Confirm `add-electron-test-signal` has landed: `tsconfig.electron.json` exists, `npm run build` runs it, and the import-graph test is in the suite. Do not start the split without this — it is the only gate that catches a fumbled import or a missing export.
- [ ] 1.2 Record the starting baseline for later comparison: `wc -l electron/main.mjs` (expect 4297), and the full sorted list of channel names from `grep -o 'ipcMain\.\(handle\|on\)("[^"]*"' electron/main.mjs` (expect 47). Save both to the change folder — the channel list is the acceptance evidence for "IPC surface unchanged".
- [ ] 1.3 Confirm both gates are green on a clean tree before touching anything.
- [ ] 1.4 Establish the per-commit rule for the whole change: one module per commit, both gates green before the next, moved code kept verbatim including comments. Do not squash — per-commit revertibility is a stated deliverable (design, Migration Plan).
  - **Three tasks are explicit exceptions to "verbatim"** and must each get their own commit and closer review: task 3.1 (the `runQueue` thunk conversion changes call shape at every collaborator), task 3.6 (the orphans move to a different module than their block position implies), and task 4.5 (converting live-session's `ListenMode` field writes into named transitions, design D11). Everywhere else, a reviewer should be able to confirm a commit is a pure move.
- [ ] 1.5 Note the two module-scope side-effect calls that the block table does not show: `main.mjs:74–75` calls `loadEnvFile()` and `logPoBillingPathOnce()` at import time. `loadEnvFile()` must run before any `envFlag(...)` initializer — `let promptReviewMode = envFlag(...)` at `:1307` is evaluated at module scope — and before binary/env resolution. After the split these become explicit ordering in the wiring block, with the same silent-failure profile as the security ordering in task 4.2. Record where each lands before starting group 2.

## 2. Tier 1 — stateless, Electron-free (~1400 lines, near-zero risk)

Each module in this group: create the file with a header comment stating its single responsibility, move the code verbatim, wire it in `main.mjs`, add its `.test.mjs`, confirm both gates green, commit. No pragma or typing step is needed — `tsconfig.electron.json`'s include glob covers every new module automatically (design D5).

- [x] 2.1 `electron/gemini-tools.mjs` — move `buildPipelineToolDeclarations`, `buildAlwaysToolDeclarations`, `buildClaudeTools`, `buildLiveTools` (block 13, ~180 lines after the capability tier pulls its share out — D1). Test: declaration shape is stable, and the pipeline-gated declarations are present only when the pipeline is available.
- [x] 2.2 `electron/gemini-prompts.mjs` — move `buildSystemInstructionText`, `buildListenSystemInstructionText`, `buildListenEntryConfirmationPrompt`, `buildListenExitSynthesisPrompt` (block 13, ~180 lines after the capability tier pulls its share out — D1). Test: converse vs listening instructions differ in the documented ways; interpolated values (user name, workspace) appear.
- [x] 2.3 `electron/pipeline-probes.mjs` — move `assertExecutable`, `claudeBinary`, `openspecBinary`, `hasOpenSpec`, `openChangesWithTasks`, `claudeWorkdir`, `checkClaudeStatus`, `probePipelineAvailability`, `checkOpenSpecStatus`, `checkSkillsStatus`, `checkAgentsStatus`, `checkClaudeHealth`, and ownership of `pipelineAvailable` (block 6, ~226 lines). Test with an injected binary-resolver fake — must spawn no real subprocess and must not require `claude` on PATH. Cover `assertExecutable` rejecting a non-executable candidate, since that is a security boundary.
- [x] 2.4 `electron/pipeline-install.mjs` — move `globalAgentsDir`, `installedAgentFile`, `bundledResourceDir`, `personasSourceDir`, `skillsSourceDir`, `installIrisAgents`, `pathExists`, `installPipelinePrereqs`, `ensureProjectScaffold`, `agentDescription`, `latestOpenChange`, `agentsSnapshot` (block 9, ~299 lines). Test with an injected filesystem fake. Cover that `RETIRED_AGENTS` files are cleaned up on install — easy to lose in a move and invisible afterwards.
- [x] 2.5 `electron/user-config.mjs` — move `parseEnvFile`, `loadEnvFile`, `envFlag`, `envNumber`, `sleepDelayMs`, `activityEmitIntervalMs`, `shutdownDeadlineMs`, `promptReviewTimeoutMs`, `userConfigPath`, `ensureIncludes`, `getFullConfig`, `serializeConfigValue`, `assertConfigValueIsSafe`, `writeUserConfig`, `setPromptReviewMode`, `savePoToken`, `testGeminiKey`, `previewVoice`, and ownership of `promptReviewMode` and `previewSession` (blocks 1 + 7, ~305 lines). The one Electron reference in block 7 becomes an injected dependency. Test: `assertConfigValueIsSafe` rejects the values it is meant to reject, and `savePoToken` never writes a token into a tracked file — both are security boundaries per the `config-persistence` capability.
- [x] 2.6 Define the capability contract before any capability module exists (design D10): a factory returning `{ toolDeclarations, promptFragment, ipcHandlers, probe, teardown }`, with every field optional. Write it down in one place with a comment, and add the composition points — `gemini-tools.mjs` concatenates declarations, `gemini-prompts.mjs` splices fragments — as no-op compositions over an empty capability list. Landing the seam before the first user keeps groups 2 and 6 independent.
- [x] 2.7 Checkpoint: `main.mjs` is ~2900 lines, `npm run build` is green (so all six new modules are clean at the typecheck floor), each has a passing test, and both halves of the import-graph test picked all six up with no edit to either. If the supply-side test needed an edit to see them, its discovery rule is wrong — fix the rule, not a list. If the demand-side check did not validate `main.mjs`'s new imports of them, that is a defect in the check, not in the split.

## 3. Tier 2 — stateful, Electron-free (~1450 lines)

Same per-module routine as group 2. These modules own mutable state, so their tests must cover the state transitions, not just the happy path.

- [x] 3.1 **Resolve the hoisting hazard first, before moving any tier-2 code.** `main.mjs:529` builds `runQueue` at module scope referencing `startClaudeRun` and `announceClaudeCompletion`, relying on same-file function hoisting — a guarantee that vanishes once they live in different modules, producing an ESM cycle (`run-dispatch` → `run-exec` → `announcements` → `live-session` → `run-dispatch`) that resolves to `undefined` at runtime rather than erroring. Restructure the wiring so the queue is constructed in `main.mjs` after all imports, with every collaborator passed as a thunk or accessor rather than a direct reference. Verify by submitting a task end to end and by confirming a run's completion is still announced. This is the highest-risk step in the change.
- [x] 3.2 `electron/renderer-bridge.mjs` — move `emitToRenderer`, `emitEvent`, `flushTranscripts`, and ownership of `userTranscriptBuffer`, `modelTranscriptBuffer`, `irisUiContext` (the generic parts of block 3, ~90 lines). Takes an injected window accessor so it never imports Electron. Test: emits are dropped safely when no window exists. **Leave block 3's canvas pieces** — `requestCanvasImage`, `maybeStartCanvasMcp`, `ensureCanvasMcpForRun`, `pendingCanvasImageRequests`, `canvasEngaged` — in `main.mjs` for now; task 5.1 collects them into the canvas capability module rather than routing them through a generic bridge they do not belong to.
- [x] 3.3 `electron/session-store.mjs` — move `SESSION_STORE` constants, `normalizeWorkstream`, `loadSessionStore`, `persistSessionStore`, `findWorkstream`, `sessionsSnapshot`, `emitSessions`, `projectSessionLabel`, `isAutoLabel`, `createWorkstream`, `activeWorkstream`, `selectWorkstream`, `setWorkstreamCwd`, `setWorkstreamAgent`, `setAgentModel`, `AGENT_ROSTER`/`MODEL_CHOICES`/`MODEL_DEFAULTS`, `resolveAgentModel`, `agentKey`, and ownership of `sessionStore` (block 4, ~410 lines). Test the **corrupt-store quarantine and schema-version branches explicitly** — a cold path no manual smoke run will reach, and one the `config-persistence` capability requires.
- [x] 3.4 `electron/announcements.mjs` — move `notifyIris`, `neutraliseUntrustedMarkers`, `fenceUntrustedText`, `drainPendingAnnouncements`, `announceAgentSelection`, `workspaceInfo`, `workspaceContextLine`, `announceWorkspaceUpdate`, `userDisplayName`, `announceClaudeCompletion`, `sendContextSupplement`, and ownership of `pendingClaudeAnnouncements` (block 5 + parts of 12, ~150 lines). Test the buffer-when-offline / redeliver-on-reconnect behavior the `session-announcements` capability requires, and that `neutraliseUntrustedMarkers`/`fenceUntrustedText` still sanitize — a prompt-injection boundary.
- [x] 3.5 `electron/run-dispatch.mjs` — move `rememberClaudeSessionId`, `pushActivity`, `pushToolStart`, `pushToolEnd`, `handleClaudeStreamEvent`, `emitTaskReviewEvent`, `notifyTaskReviewParked`, `notifyTaskReviewResolved`, `buildRun`, `dispatch`, `submitClaudeTask`, `cancelTaskReview`, `approveTaskReview`, `respondToTaskReview`, `resolvePromptReview`, `startNewClaudeSession`, `getClaudeTaskStatus`, `stopClaudeTask`, `setAgentModelTool`, `getUiContext`, `controlUi`, `executeClaudeTool`, `defaultPoAnswers`, `emitPoQuestionEvent`, `askUserQuestionViaVoice`, `resolvePendingPoQuestion` (blocks 8 + 12, ~380 lines). Test the prompt-review gate's approve/cancel/timeout paths and that a pending PO question settles exactly once — both are existing capability requirements.
  - **Deviation, recorded rather than hidden**: the verbatim move measured 670 lines, over the 450-line ceiling (the task's own ~380 estimate undershot). Split into two modules: `run-dispatch.mjs` (449 lines — review gate + tool-execution surface: `PendingReview` and everything through `executeClaudeTool`) and `electron/run-stream.mjs` (261 lines — run activity/tool-step stream projection + PO live-question relay: `PendingQuestion`, `rememberClaudeSessionId`, the activity throttle, `pushActivity`/`pushToolStart`/`pushToolEnd`, `handleClaudeStreamEvent`, `defaultPoAnswers`, `emitPoQuestionEvent`, `askUserQuestionViaVoice`, `resolvePendingPoQuestion`). `run-dispatch.mjs` takes run-stream's `resolvePendingPoQuestion` as an injected dependency for its `answer_po_question` tool case. Both land with their own `.test.mjs`; the settle-once and prompt-review-gate coverage this task calls for is split across the two accordingly.
- [x] 3.6 **Assign the three orphans explicitly** — code that no other task names and that would otherwise be swept along with whatever block it physically sits in. All three are hazardous precisely because every automated signal this plan builds would still pass:
  - `PendingQuestion` (`main.mjs:322–378`) sits inside block 2 by position, so it would land in `listen-mode.mjs` — while every one of its mutators goes to `run-dispatch.mjs`. **Resolved to `electron/run-stream.mjs`** (not `run-dispatch.mjs` as originally planned here — task 3.5's module split, decided after this task was written, put the PO live-question relay and its mutators in run-stream.mjs instead). Its "raised → answered/expired/abandoned, exactly once" invariant is governed by `voice-decision-relay`; covered by a settle-once test in `run-stream.test.mjs` (answered-vs-expired race, recommended-option-on-timeout).
  - `PendingReview` (`main.mjs:2463–2506`) sits inside block 11 by position, so it would land in `run-exec.mjs` — again severed from its mutators. **Resolved to `electron/run-dispatch.mjs`** with the prompt-review gate, as planned. Covered by a settle-once test in `run-dispatch.test.mjs` (approve, cancel, and timeout paths).
  - `chooseWorkstreamCwd` (`main.mjs:995–1009`) is unassigned and contains `dialog.showOpenDialog` — one of only two Electron call sites in the whole non-Electron-confined region (the other, `app.isPackaged`, was handled by task 2.5). **Resolved to `session-store.mjs`**, with `showOpenDialog` and `getMainWindow` received as injected dependencies — the spec's own sanctioned pattern for a domain module that needs a native dialog. Covered by three tests: applies the chosen folder, leaves cwd unchanged on cancel, and falls back to the active workstream for an unknown id.
- [x] 3.7 `electron/run-exec.mjs` — move `killChild`, `startClaudeRun`, `startDevRun`, `startPoRun`, `runProjectDir` (block 11, ~400 lines). Test with injected spawn and PO-session fakes; spawn no real subprocess. Cover that DEV is spawned detached and killed by process group, and that `computeWorkerEnv`'s withholding of `GEMINI_API_KEY` from both roles and `CLAUDE_CODE_OAUTH_TOKEN` from DEV is still applied — a security boundary that a move could silently drop.
- [x] 3.8 Checkpoint: `main.mjs` is ~1450 lines. Confirm no tier-2 module imports another tier-2 or tier-3 module for a runtime value — cross-module references must be injected accessors resolved in `main.mjs` (design D6). Grep the new modules' import blocks and check by eye.
  - `main.mjs` measured at 1850 lines (design's tier-2 target was ~1450; the run-dispatch/run-stream split and the extra wiring it required account for the difference). Grepped every tier-2 module's import block: each imports only Node builtins and pre-existing sibling modules (po-session.mjs, atomic-file.mjs, task-review.mjs, run-queue.mjs, claude-stream.mjs, coalesce.mjs, worker-env.mjs) or pure/stateless exports from tier-1 `user-config.mjs` (envFlag-style readers, not a factory instance). No module imports another new module's `createX` factory — the only `createX` import anywhere is run-stream.mjs's `createTrailingThrottle`, from the pre-existing coalesce.mjs. Every cross-module runtime value (session store, run queue, renderer emission, announcements) is wired as an injected accessor in main.mjs.
- [x] 3.9 Confirm the demand-side graph check now validates every import `main.mjs` makes of a tier-1 or tier-2 module. This is the check that covers the wiring block where 3.1's hazard lives, and it is the only automated coverage that surface has.
  - Confirmed: `electron-graph.demand.test.mjs` statically parses every `.mjs`/`.cjs` file under `electron/`, including `main.mjs`, and validates each relative import resolves to a real export of its target — covering every tier-1/tier-2 import main.mjs makes, with no per-module edit needed as new modules were added.

## 4. Tier 3 — the Electron edge (~1250 lines)

- [x] 4.1 `electron/renderer-security.mjs` — move `APP_DEV_URL`, `APP_PACKAGED_URL`, `isAppOwnDocument` (lines 3767–3773), the `app.on("web-contents-created")` navigation containment (4018–4028) and `session.defaultSession.setPermissionRequestHandler` (4035–4038) into one module exporting `installRendererSecurity()` (~60 lines). May import Electron. Deliberately below the 250 floor — a security boundary with its own capability spec should be locatable in one file (design D7). Test `isAppOwnDocument` directly: it must accept the dev URL, the dev URL with a path, and the packaged `file://` URL, and reject everything else including other `file://` paths — the exact-match-not-wildcard property the module comment calls out.
  - 74 lines. `createWindow()` (still in main.mjs, moving to window.mjs in 4.3) also needed `APP_DEV_URL` for `loadURL` — exposed as `appDevUrl`/`appPackagedUrl` on the returned interface rather than duplicated. Supply-side import-graph test's `EXPECTED_ELECTRON_DEPENDENT` updated to include this module by name, as its own comment requires. Tests mock the `electron` module (vi.mock) since this is one of the four modules allowed to import it directly; cover all four `isAppOwnDocument` cases plus navigation containment and permission scoping end to end.
- [x] 4.2 Wire `installRendererSecurity()` into `main.mjs`'s startup **before** `createWindow()`, with a comment stating the failure mode. This ordering is currently free (lexical position, 4018 vs 4232 in one `.then()`) and becomes a real contract after the split. Reversing it fails silently: `web-contents-created` registered after a window is created never fires for that window, so the app's only window loses navigation containment with no error, no failing test and no log line. Verify by hand: activate an external link in the galaxy view — which renders untrusted wiki-ingested content — and confirm the OS browser opens rather than the window navigating.
  - Wired at the exact spot the two handler registrations used to occupy — still before all 47 IPC registrations and `createWindow()`, which are untouched and unmoved. The manual galaxy-view verification is deferred to the full smoke pass (task 6.8), where it's explicitly listed as one of the three added smoke steps; automated coverage of the ordering contract itself lives in `renderer-security.test.mjs`.
- [x] 4.3 `electron/window.mjs` — move `createWindow`, `enterHud`, `exitHud`, `toggleHud`, `updateTrayMenu`, `createTray`, `hudHotkey`, `muteHotkey`, `listenHotkey`, `installAppMenu`, and ownership of `mainWindow`, `uiMode`, `deckBounds`, `tray` (block 17 minus `isAppOwnDocument`, ~200 lines). May import Electron. Keep `webPreferences` byte-identical — `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`, `backgroundThrottling: false`, and the `preload` path — and keep the frameless/transparent-from-birth construction, which cannot be toggled after creation. Verify the HUD shape morph and `setIgnoreMouseEvents` island behavior by hand. Note `hudHotkey`/`muteHotkey`/`listenHotkey` stay here as config readers; their `globalShortcut.register` calls do not (design D9).
  - 273 lines. `mainWindow`/`uiMode` are read externally in ~15 call sites still in main.mjs (requestCanvasImage, several IPC handlers, the canvas-native-file dialogs, win:control) — added `getMainWindow()`/`getUiMode()` wrapper functions in main.mjs around `windowModule`'s accessors so those call sites read through the same pattern rather than reaching into the module's internals directly. `iconPath` (used only by `installAppMenu`, alongside `appIcon` which stays in main.mjs) is injected rather than recomputed. `webPreferences` and the frameless/transparent construction moved verbatim. Supply-side import-graph test's `EXPECTED_ELECTRON_DEPENDENT` updated to include this module. HUD shape morph and `setIgnoreMouseEvents` covered by automated tests against a mocked `electron`; manual verification deferred to the smoke pass (task 6.8).
- [ ] 4.4 `electron/live-session.mjs` — move `sendWelcomeGreeting`, `startLive`, `connectLive`, `scheduleReconnect`, `handleToolCall`, `handleLiveMessage`, `stopLive`, `sendAudioChunk`, `sendCommand`, `buildLiveConfigForMode`, `logPoBillingPathOnce`, `GreetGate`, and ownership of `liveSession`, `ai`, `liveStatus`, `resumptionHandle`, `userStopped`, `reconnectAttempts`, `reconnectTimer`, `speakerMuted` (blocks 14 + 16, ~315 lines). Move the pinned identifiers verbatim — model `models/gemini-3.1-flash-live-preview`, voice `Zephyr`, 16 kHz send / 24 kHz receive, `sendRealtimeInput`. **Test `scheduleReconnect` explicitly** (backoff, attempt cap, that a user-initiated stop suppresses reconnect) — the coldest path in the app and the one most likely to break silently.
- [ ] 4.5 **Convert live-session's writes to listen-mode state into named transitions** (design D11). This is the only task in the change that is not behavior-preserving by construction, so give it its own commit and its own review. `connectLive` currently assigns `ListenMode.deliberateReconnect`, `.transitioning`, `.boundaryInFlight`, `.segmentRecord` and `.synthesizeOnNextConverseConnect` directly (lines 3370–3423), and `handleLiveMessage` reads four fields at six interleaved points. Replace the assignments with named transitions owned by `listen-mode.mjs` — e.g. `consumeDeliberateReconnect()`, `captureSegmentForSynthesis()`, `settleBoundary()` — leaving reads as accessor reads. Test each transition individually; the existing `listen-boundary.test.mjs` does not cover them.
- [ ] 4.6 `electron/listen-mode.mjs` — move `listenChunkMs`, `setListenEngaged`, `clearListenRotationTimer`, `armListenRotationTimer`, `resetListenModeSilently`, `notifyTurnComplete`, `onTurnComplete`, `notifyFreshResumptionHandle`, `onFreshResumptionHandle`, `notifyLiveClosed`, `waitForLiveClose`, `closeLiveSessionDeliberately`, `makeBoundarySession`, `driveTurnAndWaitForCompletion`, `enterListenMode`, `exitListenMode`, `runListenRotation`, `toggleListenMode`, and ownership of the three listener arrays, the engaged flag and the rotation timer (blocks 2 + 15, ~420 lines). Keeps using the existing `listen-boundary.mjs`. **Test `runListenRotation`** including the stale-resumption-handle case `listen-boundary.test.mjs:73` already describes.
- [ ] 4.7 `electron/ipc.mjs` — move the 47 `ipcMain.handle`/`on` registrations, and **only** those, out of `app.whenReady()` (lines 4034–4221, **~187 lines**, not the whole 312-line block). Handlers marshal arguments and delegate only; no logic inline. May import Electron. Note this lands below the 250 floor, which is correct — the rest of block 18 is startup sequence and security, not IPC. Preserve `handle` vs `on` exactly: `listen-mode:toggle-request`, `canvas:activate`, `secondbrain:activate` and `secondbrain:deactivate` are `on` (fire-and-forget, no reply); converting one to `handle` or back changes the renderer contract.
- [ ] 4.8 Verify the IPC surface is byte-identical: regenerate the sorted channel-name list and diff against the baseline saved in 1.2. It must match exactly — 47 channels, same names, same `handle`/`on` split. Then diff the channel set against `electron/preload.cjs`'s `window.iris` surface and confirm every preload call has a registration and no registration is unreachable.
- [ ] 4.9 Reduce `main.mjs` to its final form: imports, the wiring block, the `app.whenReady()` startup sequence, `shutdownTeardown`, and the `will-quit`/`before-quit`/`window-all-closed` handlers. Keep the startup sequence as one clearly-commented function rather than scattering it through wiring — its internal order is load-bearing throughout: platform refusal gate first (it calls `app.quit()`), then dock icon and `installAppMenu()`, then the availability probes, then `installRendererSecurity()`, then `createWindow()`, then `createTray()`, then the three `globalShortcut.register` calls, then `app.on("activate")`. Confirm ~180–230 lines. If wiring alone pushes it past ~250, extract `electron/wiring.mjs` — explicitly permitted.
- [ ] 4.10 Keep `shutdownTeardown` in `main.mjs` with its six steps in their existing order — `stopLive()`, group-kill every DEV child from `runQueue.list()`, `closeAllPoSessions()`, `canvasStore.flush()`, `canvasMcp.stop()`, `notesVaultGraph.stop()`. Do not convert these to self-registered per-module hooks: the order is deliberate and hook order would become incidental (design D8). Keep `globalShortcut.unregisterAll()` on `will-quit` beside the registrations it pairs with, and keep the `before-quit` deadline race intact — the `app-shutdown` capability requires the whole sequence to race one deadline before `app.exit(0)`.

## 5. Capability tier — vertical slices

These modules pull code *out* of the layer modules created in groups 2–4, so they land last, when every piece they gather already has a home and a test. Each is one commit.

- [ ] 5.1 `electron/capabilities/canvas.mjs` (~180 lines) — gather what is currently spread across four future layer modules: `requestCanvasImage`, `maybeStartCanvasMcp`, `ensureCanvasMcpForRun` and ownership of `canvasEngaged`/`pendingCanvasImageRequests` (block 3); the canvas tool declarations (block 13); the `canvas:*` IPC handlers (block 18); and the canvas flush + MCP stop teardown steps. Existing `canvas-store.mjs` and `canvas-mcp.mjs` are unchanged — this is the main-process integration layer above them. Remove each piece from its layer module in the same commit, so nothing is duplicated.
- [ ] 5.2 `electron/capabilities/second-brain.mjs` (~230 lines) — gather `checkNotesSkillsStatus`, `ensureNotesVaultReady`, `renderNotesVaultConfig`, `vaultChangedSince`, `probeSecondBrainAvailability` and ownership of `secondBrainAvailable` (block 10); the notes/galaxy tool declarations (block 13); the `secondbrain:*` IPC handlers (block 18); and the vault-graph stop teardown step. Existing `vault-graph.mjs` and `vault-graph-parse.mjs` are unchanged.
- [ ] 5.3 Confirm the composition actually composes: `gemini-tools.mjs` and `gemini-prompts.mjs` contain no canvas- or second-brain-specific declaration or prose, and `ipc.mjs` registers the capability handlers by iteration rather than by name. If a capability string is still hardcoded in a core module, the seam is not real.
- [ ] 5.4 Confirm teardown still runs in the required order: the core sequence explicitly (`stopLive`, group-kill DEV children, `closeAllPoSessions`), then the capability teardowns as a group. The three capability steps already run last today and are mutually independent, which is what makes the group safe (design D8).
- [ ] 5.5 Update the import-graph test's discovery to `electron/**/*.mjs` so the capabilities subdirectory is covered, and update its expected-count guard. A capability module escaping coverage because the glob was never widened is exactly the silent-omission failure the guard exists to prevent.
- [ ] 5.6 Sanity-check the decomposition against its own purpose: pick the most recent feature commit and confirm that, under the new layout, it would have landed in one capability module plus a registration rather than across six layer modules. If it would not, the capability boundary is in the wrong place — record that rather than pretending otherwise.

## 6. Verify

- [ ] 6.1 Confirm the size convention: no non-test file under `electron/` exceeds 450 lines, and `main.mjs` is ~150–250.
- [ ] 6.2 Confirm the Electron confinement rule: only `main.mjs`, `ipc.mjs`, `window.mjs`, `renderer-security.mjs` (and `preload.cjs`, out of scope) import `electron` or touch an Electron API. Grep every other module. Two traps: `process.resourcesPath` is *not* an Electron import and is fine in a domain module (`pipeline-install.mjs` uses it), whereas `app.isPackaged` *is* — `user-config.mjs`'s `userConfigPath` and `window.mjs`'s prod-load check both use it, and the former must receive it injected.
- [ ] 6.3 Confirm packaging is unaffected: `package.json`'s `build.files` is `["dist/**", "electron/**", "!electron/**/*.test.mjs", …]`, so all ~15 new modules ship automatically and no build config edit is needed. Verify no non-test helper file was added under `electron/` that should not ship, and that `tsconfig.electron.json` remains outside `files`.
- [ ] 6.4 Confirm no shared-state module exists and each item in D2's state-ownership table has exactly one owning module, per the design's ownership table.
- [ ] 6.5 Confirm every new module has a passing `.test.mjs` and is covered by the import-graph test without that test having been edited to name it.
- [ ] 6.6 Both gates green: `npm run build` (both TypeScript projects plus Vite) and `npm test`. A green build means all 17 new modules are clean at the typecheck floor, since the include glob covers them with no opt-in.
- [ ] 6.7 Confirm both halves of the import-graph test still hold after the split, and specifically that the supply-side test's candidate-count guard was updated to the new expected number — the split changes that count by ~17, so a stale guard either fails loudly (fine) or was loosened (not fine). Confirm `ipc.mjs` and `window.mjs` are named as intentional exclusions rather than silently dropping out via the `import "electron"` text rule.
- [ ] 6.8 Run the full manual smoke path from the spec: launch, wake, hold a voice turn, submit a task through the review gate, answer a PO question by voice, cross the PO→DEV gate, switch workstream, choose a project folder, enter and exit HUD mode, enter and exit listening mode through at least one rotation, mute, quit cleanly. Add three steps the previous smoke list missed, each covering a silent failure mode: **activate an external link in the galaxy view** and confirm the OS browser opens rather than the window navigating (D7's hazard); **confirm the mic permission prompt still scopes to the app's own document**; and **exercise all three global shortcuts** (HUD, mute, listening) after the registration move.
- [ ] 6.9 Exercise the cold paths deliberately, since no smoke step reaches them: force a Live reconnect (kill connectivity mid-session) and confirm buffered announcements are redelivered; corrupt `~/.iris/claude-sessions.json` and confirm it is quarantined rather than parsed-and-overwritten; quit during an active DEV run and confirm `shutdownTeardown` bounds and the process group is killed.
- [ ] 6.10 Confirm every existing capability spec is still true against its current text with no edit. If any needs rewording, the split changed behavior — fix the code, not the spec.

## 7. Documentation and hand-off

- [ ] 7.1 Rewrite `CLAUDE.md`'s File map for the new layout. The current claim that the bridge "lives almost entirely in `electron/main.mjs` (~1500 lines)" is false after this change; replace it with the module list and one line each. Keep `CLAUDE.md` a router per its own docs-discipline convention.
- [ ] 7.2 Update `docs/ARCHITECTURE.md` and `docs/PIPELINE_INTERNALS.md` file references to point at the new modules instead of `main.mjs`.
- [ ] 7.3 Update `docs/TESTING.md` for the new module tests and the updated graph-test candidate count. Keep its file references as globs rather than enumerations, per the convention `add-electron-test-signal` established there.
- [ ] 7.4 Update the "Known gaps" section of `docs/TESTING.md`: the vitest Electron-stub harness is now worth reconsidering, since `main.mjs` is ~200 lines and `ipc.mjs` a flat registration list — this is the coverage that would close the wiring gap 3.1 identifies, and `add-electron-test-signal`'s D5 explicitly deferred it to this point. Also carry forward the three deferred strictness flags (re-measure their prices post-split; `strictNullChecks` is the one to prioritise, and `live-session.mjs` and `window.mjs` are where it pays) and the remaining over-450 files (`src/App.tsx` 1738, `src/components/SetupPanel.tsx` 1023, `src/components/VaultGalaxy.tsx` 561, `electron/canvas-mcp.mjs` 557).
- [ ] 7.5 Archive the change so `main-process-structure` syncs into `openspec/specs/`.
