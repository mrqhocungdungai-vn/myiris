## 1. One verb, two depths

- [x] 1.1 `investigate` declares a required `depth` enum; `model`/`skills`/`structuredOutput`/`clause` resolve from it
- [x] 1.2 `review` removed from the registry and from the renderer's display vocabulary
- [x] 1.3 `depth` carried on the run and through the review gate; defaults to `explain`, never to the expensive depth
- [x] 1.4 Tests for both depths, the default, the schema requirement, and the write ban; five wires cut

## 2. Gates

- [x] 2.1 build / test / lint / scan:secrets / spec:check green — 1800 tests
