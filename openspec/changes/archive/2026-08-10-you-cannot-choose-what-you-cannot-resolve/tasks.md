## 1. Separation is intent

- [x] 1.1 `pickZoomTarget` rejects a challenger within `ZOOM_SWITCH_SEPARATION_PX` of the incumbent on screen
- [x] 1.2 Threshold set above what the existing bias and occlusion rule already block, so it does something
- [x] 1.3 Three tests: inseparable neighbour refused, same neighbour taken once close, distant note still taken
- [x] 1.4 Cut the wire — red

## 2. Gates

- [x] 2.1 build / test / lint / scan:secrets / spec:check green — 1748 tests
