## 1. Vendor the LLM-Wiki skill snapshots

- [x] 1.1 Fetch `vanillaflava/llm-wiki-skills` at latest `main`; record the resolved commit SHA for attribution. (Default branch is actually `master`, not `main`. Resolved to release [`v0.5.0`](https://github.com/vanillaflava/llm-wiki-skills/releases/tag/v0.5.0), commit `ff6f7be1a0e397192a1ac4a0d06d298062e9b208` — happened to be `master`'s tip at vendor time too, but the release tag is the more precise, stable citation and is what ATTRIBUTION.md records. Content vendored from the user-supplied local copy at that same release, confirmed byte-identical via `diff -rq` against the git-cloned commit.)
- [x] 1.2 For each of the 6 skills (`wiki-config`, `wiki-ingest`, `wiki-query`, `wiki-lint`, `wiki-integrate`, `wiki-crystallize`), verify it is a directory containing a valid `SKILL.md`, and copy that directory verbatim (no content edits) into `resources/skills/claude-skills/`.
- [x] 1.3 Add the upstream MIT license as `resources/skills/claude-skills/LICENSE-vanillaflava-llm-wiki-skills` (same directory level as the existing `LICENSE-mattpocock-skills` / `LICENSE-openspec` — one level deeper than `resources/skills/`, not directly under it).
- [x] 1.4 Add a row to `resources/skills/ATTRIBUTION.md` for the LLM-Wiki skills: upstream source URL, pinned commit SHA, MIT license, and the re-vendor/refresh steps.

## 2. Pin the vault to `~/iris-second-brain`

- [x] 2.1 In `electron/main.mjs`, add an "ensure `~/iris-second-brain` exists" step that runs before a plain-Claude (no-agent) run is started (create the directory if absent; never touch it if present). Implemented as `ensureNotesVaultReady()`.
- [x] 2.2 On the same step, pre-seed `~/iris-second-brain/wiki-config.md` and `~/iris-second-brain/wiki-schema.md` if either is absent (never overwrite if present). This closes a blocking gap found while vendoring (see design.md D5): the operational wiki skills' "Config Discovery" step, on a genuinely first-ever run, ends the turn asking the user to run `/wiki-config` interactively — which a one-shot `claude -p` run has no way to answer. Generate `wiki-config.md` from the vendored `resources/skills/claude-skills/wiki-config/assets/wiki-config-template.md`, with one required deviation: write `blacklist: []` instead of copying the template's placeholder prose ("Folder(s) where the wiki should not write") — `wiki-config`'s own Validate step flags leftover placeholder text, and `~/iris-second-brain` is single-purpose so an empty blacklist is correct, not a stub. Keep `index_excludes`/`templates_folder` as forward-slash paths (`raw/`, `archive/`, `ingested/`, `templates/` — macOS only, per Iris's platform constraint). Copy `wiki-schema.md` verbatim from the same assets dir (no placeholders in it, no edits needed). Do NOT pre-seed `Home.md`/`Overview.md`/`index.md`/`log.md`/`raw/`/`ingested/`/`templates/` — Config Discovery only gates on the two files above, and the operational skills already fall back gracefully when `templates/` is missing. Implemented as `renderNotesVaultConfig()`; transform verified against the real vendored template (regex output checked directly, produces `blacklist: []` and forward-slash paths as intended).
- [x] 2.3 In `startDevRun`, the `--append-system-prompt` string is currently built once and shared unconditionally across plain-Claude and DEV runs (role only branches later, via the separate `--agent` flag push) — there is no existing plain-Claude-only branch to extend. Change the prompt-building so the vault directive is concatenated onto that string **only when `!run.agent`** (i.e., wrap the appended text in an `if (!run.agent) { ... }`, or build it as a conditional suffix): the personal-notes / LLM-Wiki vault root is `~/iris-second-brain`, and the wiki skills operate there regardless of `cwd`. When `run.agent` is set (PO or DEV), the resulting `--append-system-prompt` value MUST remain byte-identical to today's — verify this explicitly (e.g., diff the constructed string before/after for a DEV run) rather than assuming the conditional is correct. Verified with a standalone script reproducing the exact conditional: DEV/PO output equals today's base string; plain-Claude output differs (includes the vault directive).
- [x] 2.4 Confirm the vendored snapshots are left unedited (the vault is pinned only through the append-system-prompt seam and the 2.2 pre-seed step, not by modifying any `SKILL.md`). Confirmed — `ensureNotesVaultReady()`/`renderNotesVaultConfig()` only read from `resources/skills/claude-skills/wiki-config/assets/` and write into `~/iris-second-brain`, never into the vendored skill directories.
- [x] 2.5 The append-system-prompt directive is a soft instruction, not a sandboxed writable root — after a plain-Claude capture-note run completes, check whether a new/modified file actually exists under `~/iris-second-brain` since the run started, before Iris confirms the save to the user. If no matching file is found, report the capture as failed/unverified rather than assuming success (see the corresponding scenario added to `specs/personal-knowledge-notes/spec.md`). Implemented as `vaultChangedSince()` + `NOTE_CAPTURE_HINT_RE`, scoped to plain-Claude tasks whose text plausibly requests a note capture, so unrelated tasks never get a spurious caveat.
- [x] 2.6 Vault creation (2.1/2.2) and skill installation (`installPipelinePrereqs()`, the SetupPanel's "Install missing" button) are independent actions on independent schedules — a user can have `~/iris-second-brain` already created but the 6 `wiki-*` skills not yet installed in `~/.claude/skills`. Before appending the 2.3 vault directive, check `checkNotesSkillsStatus()` (all 6 names present under `~/.claude/skills` — a separate check from `REQUIRED_SKILLS`, which is PO/DEV-only). If not installed, append a different directive instead: tell Claude the notes skills aren't installed, and if the user asks to capture/retrieve a note, to say so and point at "Install missing" rather than attempting an ad-hoc, ungoverned note write (see design.md D6 and the corresponding scenario added to `specs/personal-knowledge-notes/spec.md`).
- [x] 2.7 The SetupPanel's Claude section (openspec CLI / Global skills / Iris agents rows) had no row at all for the LLM-Wiki skills — a user had no visible way to check install state without asking Claude directly (found from a screenshot during manual review). Thread `checkNotesSkillsStatus()` through `checkClaudeHealth()`'s IPC response (`notesSkillsOk` / `missingNotesSkills` / `notesSkillsInstallHint`, parallel to the existing `skillsOk`/`agentsOk`) and add a `PrereqRow` — "Second-brain notes (LLM-Wiki skills)" — to `SetupPanel.tsx`, alongside the existing three. Its missing state also joins the "show Install missing button" condition (see design.md D7 and the corresponding scenario in `specs/personal-knowledge-notes/spec.md`). Informational only — `notesSkillsOk` false never blocks PO/DEV, consistent with these skills staying out of `REQUIRED_SKILLS`.

## 3. Verify install and non-prerequisite behavior

- [x] 3.1 Confirm `installPipelinePrereqs()` copies the 6 new skill directories into `~/.claude/skills` with no change to the installer loop (it already iterates `resources/skills/claude-skills/`). Confirmed by code inspection — the loop iterates all directory entries under `claude-skills/` generically; the 6 `wiki-*` dirs need no special-casing.
- [x] 3.2 Confirm `REQUIRED_SKILLS` and `checkSkillsStatus()` are unchanged, so the SetupPanel does not flag the LLM-Wiki skills as a missing pipeline prerequisite. Confirmed — `REQUIRED_SKILLS` still lists exactly the original 7 entries; no `wiki-*` added.
- [x] 3.3 Confirm the copy-only-where-missing / symlink-safe policy still holds for the new directories (an existing `~/.claude/skills/wiki-*` is skipped untouched). Confirmed by code inspection — the installer's `pathExists()`-guarded skip applies uniformly to every directory entry, not a name-specific list.

## 4. End-to-end trial (manual verification)

These steps require a human operator driving the running app (voice input, toggling whether the Claude CLI is installed) — they are a manual QA pass before archiving the change, not steps a headless DEV agent can script from tasks.md alone.

- [x] 4.1 With the Claude CLI present, run the install action and verify all 6 `wiki-*` skills land in `~/.claude/skills`. Confirmed — SetupPanel's "Second-brain notes" row (added in 2.7) went green after clicking "Install missing".
- [x] 4.2 By voice/plain-task, capture a note; confirm a markdown page is written under `~/iris-second-brain` (not under the active project `cwd`) and the vault opens as an Obsidian vault.
- [x] 4.3 In a later plain-Claude turn, retrieve/search that note; confirm the worker returns the saved content from the vault, not a fabricated answer.
- [x] 4.4 With the Claude CLI absent, confirm Iris behaves as the unchanged chat-only companion and does not claim it can save notes.
- [x] 4.5 With the Claude CLI present but *before* clicking "Install missing" (so `~/.claude/skills/wiki-*` do not exist yet), ask Iris to capture a note; confirm it tells the user the notes capability needs installing first, rather than writing an ad-hoc file or silently doing nothing.
- [x] 4.6 Try a capture using the Vietnamese example phrasing from the spec scenarios (e.g. "ghi chú lại: …", "tìm trong second-brain …"); confirm the vendored skill's (English-authored) trigger description actually fires on it. If it doesn't reliably fire, record this as a "Decision needed" rather than silently reinterpreting the requirement.

User confirmed 2026-07-23: full manual trial passed end-to-end.

## 5. Checks and docs

- [x] 5.1 Run `npm run build` (tsc --noEmit + vite build) and `npm test` (Vitest); both pass. Both green — 9 test files, 61 tests passed; no regressions.
- [x] 5.2 Refresh the bundled-skill count in `openspec/specs/pipeline-setup-install/spec.md`'s "Fresh machine gets everything in one click" scenario (10 → 16 bundled skills). This count lives only in the OpenSpec living spec — `docs/PIPELINE_GUIDE.md` / `docs/PIPELINE_GUIDE.vi.md` do not enumerate a skill count and need no edit. Full user-facing role/notes guidance is deferred to the follow-up `clarify-role-capabilities` change — do not write it here. Count independently verified: `find resources/skills/claude-skills -maxdepth 1 -type d` returns exactly 16 (10 original + 6 new `wiki-*`).
