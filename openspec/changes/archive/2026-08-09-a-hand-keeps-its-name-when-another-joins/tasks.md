## 1. A hand keeps its name

- [x] 1.1 `handIdentity` derives the key from handedness, count-independently
- [x] 1.2 `useHandControl` uses it for the EMAs and the gesture stabilizer
- [x] 1.3 Per-hand memory pruned to the hands in frame
- [x] 1.4 Tests, red when the count-dependence is reintroduced

## 2. Gates

- [x] 2.1 build / test / lint / scan:secrets / spec:check green — 1757 tests
