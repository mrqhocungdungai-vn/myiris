## 1. The lock wins

- [x] 1.1 `shouldReleaseAnchor` takes `locked` and refuses to release while it holds
- [x] 1.2 Call site passes it; omitting it is a type error
- [x] 1.3 Test red when the rule is cut

## 2. The lock is where you are looking

- [x] 2.1 A newly locked note is eased to the centre of the view, once per choice
- [x] 2.2 Suppressed while a camera drive is engaged

## 3. Gates

- [x] 3.1 build / test / lint / scan:secrets / spec:check green — 1745 tests
