## 1. The choice is the hand's

- [x] 1.1 `sightMovedEnoughToRetarget` + `SIGHT_TRAVEL_TO_RETARGET_PX`
- [x] 1.2 Travel measured from where the hand was when the target was picked, not from the previous frame, so slow deliberate movement still counts
- [x] 1.3 Tests; cut the wire — red

## 2. Gates

- [x] 2.1 build / test / lint / scan:secrets / spec:check green — 1751 tests
