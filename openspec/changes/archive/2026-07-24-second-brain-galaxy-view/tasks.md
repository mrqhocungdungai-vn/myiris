## 1. Dependencies

- [x] 1.1 Add `3d-force-graph` (vanilla) and `gray-matter` as runtime **`dependencies`** (both run/ship: `gray-matter` in main — a `devDependency` wouldn't be bundled by electron-builder, L4), pinned **exact**; add both to README's exact-identifier table (D1/D2).
- [x] 1.2 Dedupe `three` **in this order** (a second copy already exists today: `stats-gl@2.4.2 → three@0.170.0` nested under drei, so `npm ls three` prints two copies before this change — H-B): (1) add `overrides.three` pinned to the app's `three` + `resolve.dedupe: ['three']` in `vite.config`; (2) confirm the override collapses the stats-gl copy to 0.181.2 and that drei/`stats-gl`/`three-stdlib` still build and run at 0.181.2; (3) only then add the single-copy gate to `npm run build`/CI — scope it to "`3d-force-graph` resolves to the app's `three`" if a global single copy proves infeasible. `3d-force-graph@1.80.0` declares `three ">=0.179 <1"` (accepts 0.181.2). If the override breaks drei, fall back to D1(c) (M1/D2/H-B).
- [x] 1.3 **Spike gate (before any other galaxy render work — H-A):** with the pinned `3d-force-graph`, verify whether a **transparent** graph canvas + node bloom via `.postProcessingComposer()`/`UnrealBloomPass` coexist. If yes → transparent canvas over a CSS deep-space layer (task 3.2a). If no (UnrealBloomPass outputs opaque) → paint deep-space **inside the graph scene** (scene `backgroundColor` + points/skybox starfield) or use a composer-free per-node sprite/glow material (task 3.2b). Pick the mechanism here; behavior (opaque immersive + glowing nodes) is fixed either way.

## 2. Main-process vault graph owner

- [x] 2.0 In the `personal-knowledge-notes` side of `main.mjs`, expose a single authoritative **`isUserNote(vaultRelativePath)`** predicate beside `NOTES_VAULT_DIR`/`ensureNotesVaultReady` that excludes the LLM-Wiki system files (`index.md`, `log.md`, `wiki-config.md`, `wiki-schema.md`) and plumbing folders (`templates/`, `raw/`, `archive/`, `ingested/`). `vault-graph` consumes this — it does NOT re-list the plumbing (H-1). Note `index.md`/`log.md` were missing from the earlier list and MUST be covered.
- [x] 2.1 Create pure parser `electron/vault-graph-parse.mjs` (no fs, no IPC): given note file contents/relative paths (already filtered by `.md` + `isUserNote`), return a **position-free** `{ nodes, links }`. Parse frontmatter with `gray-matter` **per note inside try/catch** — malformed YAML must degrade that note to a tagless/flagged node, never abort the whole build (M-C). Parse `[[wikilink]]` forms `[[Note]]`/`[[Note|alias]]`/`[[Note#heading]]`, **skipping** matches inside fenced/inline code and `![[embed]]` transclusions (L4); node id from vault-relative path (basename for the flat vault, disambiguate by path on subfolder collision — M3); resolve link targets via a **lowercased** map (Obsidian case-insensitivity, APFS — M3); missing target → `ghost: true` node; `tags` → color group. Keep each node's absolute path as an internal field for read resolution (D3/D8/L-1).
- [x] 2.2 Create `electron/vault-graph.mjs` as a `createVaultGraph({ dir })` **factory** (like `createCanvasStore`) returning `{ getGraph, start, stop, onUpdate }` (L-3): scan the vault **asynchronously** (`fs.promises.readFile` + bounded concurrency that yields, or a `utilityProcess`/`worker_thread` — NEVER a synchronous `readFileSync`+`gray-matter` loop, which would block the Gemini Live/audio event loop on every rebuild — H1/`main-thread-budget`), keeping only files that are `.md` **and** pass `isUserNote` (M-D), and **skipping symlinked directories / tracking visited inodes** (H3). `start()` begins `fs.watch(realpath(dir), { recursive: true })` → coarse debounce → **rebuild-from-scan** → notify with the **full current position-free graph** (no delta — H-2); `start()` is **idempotent** (no-op if already watching — M3). `stop()` tears the watcher down **and clears the pending debounce timer + sets a stopped-guard** so no in-flight rebuild/async scan emits after toggle-off (M-E). Rebuild-from-scan absorbs atomic-save/rename noise and missed events; wrap the watcher in try/catch (M2).
- [x] 2.3 Vault existence + empty handling: expose whether `~/iris-second-brain` exists and whether it has **user** notes (via `isUserNote`); never create the vault from this module; only `start()` the watcher once the vault exists and the galaxy is active — availability is separate and needs no watcher (D7/M-2).
- [x] 2.4 Wire into `electron/main.mjs`: IPC `secondbrain:get-graph` (full position-free graph + availability; a fresh scan), `secondbrain:read-note` (takes a **node id**; **type/bound-check** the arg (`typeof id === 'string'`, length cap — L1), resolve against the single graph cache's node.path — never a renderer-supplied path — then `fs.realpathSync` and **assert the resolved path is inside `realpath(NOTES_VAULT_DIR)`**, refusing symlink escapes; ghost/unknown ids refused — H3/L-1/M5/D8); dedicated channel `secondbrain:graph-updated` (full graph, its own channel — L2) on debounced change; **availability rides the existing `sidecar:event` stream** (like `pipeline_availability`), emitted **only on transition** (track last value; NOT on every `ensureNotesVaultReady` call — M-5/L2). Start/stop the watcher on galaxy toggle-on/off, on window-close/app-quit, **and on `webContents` `render-process-gone` / renderer reload** (a reload doesn't fire window `"closed"` → orphaned FSEvents stream — M3).
- [x] 2.5 Expose in `electron/preload.cjs`: `getSecondBrainGraph()`, `readSecondBrainNote(id)`, and an `onSecondBrainGraphUpdated(cb)` subscription that **returns an unsubscribe closure** (mirror `onCanvasApply`); availability is read via the existing `onSidecarEvent`. The galaxy layer's `useEffect` MUST call the unsubscribe on unmount so toggles don't stack listeners (M4).

## 3. Renderer: galaxy layer + backdrop

- [x] 3.1 Galaxy layer component using `3d-force-graph`: the renderer **owns positions** — keep a `Map<id, nodeObject>` and on **every** received graph (initial `get-graph` or `graph-updated`, both full — H-2) reconcile: mutate matched ids in place (preserve `x/y/z`, update label/tag/ghost), add new ids, drop missing ones, and pass the **same object references** back to `.graphData()`. **Actively manage motion (M-B):** `.graphData()` resets the sim alpha every call, so reused refs alone only avoid *re-randomizing* — to stop the graph visibly jiggling when only metadata/one note changed, pin unchanged nodes (`fx/fy/fz`) and/or use `warmupTicks(0)`/low `d3AlphaTarget`/short `cooldownTicks`, reheating only on real topology change. Node color by tag group; ghost nodes faded and **not** openable (click disabled). **Node labels must not reach the lib's `innerHTML` tooltip (H2):** disable `3d-force-graph`'s built-in HTML tooltip or pass an HTML-escaping `.nodeLabel()` accessor, so an ingested note titled `<img onerror=…>` can't execute in the privileged renderer (verify the tooltip mechanism on the pinned lib first). **Hoist the position `Map` above the `React.lazy` galaxy layer** (in `App.tsx`/a ref that outlives the layer) so toggling off then on rehydrates positions instead of re-scrambling (M-3); reconcile safely if a graph arrives while the layer is unmounted.
- [x] 3.2 Deep-space backdrop, mechanism per the 1.3 spike (D4/H-A). **3.2a (if transparent+composer coexist):** opaque **CSS/DOM** layer behind the graph canvas — near-black `~#05060c` + radial vignette + faint drifting starfield (CSS) — with the graph canvas transparent on top; node glow via `.postProcessingComposer()` + `UnrealBloomPass`; no extra WebGL context. **3.2b (fallback):** paint deep-space inside the graph scene (scene `backgroundColor` + points/skybox starfield) with the composer bloom, or drop the composer for a per-node sprite/glow material. Either way: opaque immersive backdrop + glowing nodes, one graph canvas. Reuse `deepspace-skin` vignette/tokens where feasible (L-2).
- [x] 3.3 Empty-state: when the vault exists but has no **user** notes, show a friendly empty-state instead of a blank/broken layer (D7/H4).
- [x] 3.4 Pause the galaxy on the HUD `running` signal so an idle immersive galaxy burns 0 GPU when the HUD sleeps/loses focus (L1). NB `3d-force-graph` is **vanilla, not r3f** — use its own `pauseAnimation()`/`resumeAnimation()` (and stop the engine cooldown), NOT an r3f `frameloop` prop (L-B).
- [x] 3.5 Wrap the galaxy layer in an **error boundary** that force-closes it (sets `secondBrainActive = false`) on throw, and ensure **Esc force-closes** the layer regardless of its internal state — so a crashed WebGL layer can't leave the fullscreen click-through-disabled overlay trapping all desktop clicks (L3).

## 4. HUD integration

- [x] 4.1 Add `secondBrainActive` state and a new HUD control button beside `PenTool` (network/brain icon) in `HudShell.tsx`/`App.tsx`; render the galaxy layer only when active, lazily (`React.lazy`, like `DrawingCanvas`) (D5).
- [x] 4.2 Single active-layer invariant: enabling the galaxy sets `drawingActive = false` and enabling drawing sets `secondBrainActive = false` (D5).
- [x] 4.3 Click-through: **latch** `setHudInteractive(true)` while `secondBrainActive` (mirror the `drawingActive` early-return in the App.tsx latch effect, ~lines 405–438 — do NOT rely on per-move `elementFromPoint` sampling, which would flip mid-orbit-drag) and add `secondBrainActive` to that effect's deps array; existing cleanup `setHudInteractive(false)` reverts on close (L-A/D5).
- [x] 4.4 Availability gating + sync: show the toggle only when the vault exists (independent of `pipelineAvailable`); re-check on HUD open and react to the availability transition on the `sidecar:event` stream; on disappearance force-close the galaxy and hide the toggle (M4/M-5/L2/D7).
- [x] 4.5 App-wide navigation hardening (M1/D9): add `mainWindow.webContents.setWindowOpenHandler(() => ({ action: 'deny' }))` and a `will-navigate` handler that blocks any URL except the dev/prod app origin, routing external links via `shell.openExternal` — so a `[text](https://…)` link in an untrusted note can't top-level-navigate the single BrowserWindow away from the app.

## 5. NoteReader overlay

- [x] 5.1 Extract an inner `ReaderCore` from `ReaderOverlay` (keeps the `.reader-backdrop`/`.reader-card` DOM, drag-to-dismiss, `readerScale`/two-palm zoom, palm-scroll rAF loop, Esc/X close) accepting `{ title, body, footerHint, headerSlot }` where **`body` is a rendered node** supplied by the caller. `ReaderOverlay` passes its task badges via `headerSlot` and renders `normalizeMarkdown(task.output)` into `body` — behavior unchanged; the task-reader smoke check must still pass. **Keep `.reader-backdrop` in `App.tsx`'s click-through allowlist (~line 422)** (M6/D6).
- [x] 5.2 Create `NoteReader` on `ReaderCore` taking `{ title, markdown }`, rendering `react-markdown`+`remark-gfm` into `body` itself with **no `rehype-raw` and no `dangerouslySetInnerHTML`** (match `ReaderOverlay` exactly so raw HTML in an untrusted note stays escaped — H2) and passing no `headerSlot` (no run id / agent / status chrome); pass `hand={null}` in change #1 (rAF loop is null-safe). Open on node click, close on X/Esc; gesture bindings deferred to change #2. Enforce reader single-instance with the task reader (don't stack two) (L3/D6).
- [x] 5.3 On node click, fetch the note's markdown via `readSecondBrainNote(id)` (by id, not path) and render it; ghost nodes never trigger a read (M5/D8).

## 6. Verification

- [x] 6.0 Add `electron/vault-graph-parse.test.mjs` (vitest, following `canvas-store.test.mjs`) covering the pure parser: code-fence/inline-code and `![[embed]]` skipping, `[[Note|alias]]`/`[[Note#heading]]` collapse, case-insensitive resolution + subfolder-collision id disambiguation, ghost synthesis, and `isUserNote` exclusion of `index.md`/`log.md`/plumbing (M-1).
- [x] 6.1 `npm run build` (tsc --noEmit + vite) passes; `npm test` passes (incl. the new parser suite); the single-`three` check (1.2, `npm ls three`) passes as part of build/CI.
- [x] 6.2 Manual (running app): with a populated vault, toggling "show second brain" shows the galaxy on the opaque deep-space backdrop with node bloom; wiki plumbing files are NOT nodes; clicking a node opens the note (ghost nodes don't); adding/removing a note in Obsidian updates the galaxy live with stable positions (no reheat); galaxy and drawing are mutually exclusive; exiting restores the transparent HUD; the galaxy stops rendering when the HUD sleeps.
- [x] 6.3 Manual (chat-only / vault lifecycle): Claude CLI absent but vault present → galaxy still works; vault absent → toggle not offered and vault not created; capturing the first note (which creates the vault) makes the toggle appear without relaunch; empty vault (only plumbing) → empty-state.

## 7. Spec sync

- [x] 7.1 `openspec validate second-brain-galaxy-view` is clean; archive to sync `openspec/specs/second-brain-galaxy-view/` after implementation and verification.
